{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser-chrome",
  "description": "Minimal capability for the Browser Workbench chrome webview (toolbar/tabs UI only). Remote content webviews are deliberately listed in no capability and hold zero Tauri permissions.",
  "windows": ["browser-workbench"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://**" },
        { "url": "http://localhost:**" },
        { "url": "http://127.0.0.1:**" }
      ]
    }
  ]
}
